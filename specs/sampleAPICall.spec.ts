// import { getToken } from '../api/samplecall';
// import { getToken2 } from '../api/samplecall';
// import { getToken3 } from '../api/samplecall';
// import {getTokenAngular} from '../api/samplecall';
// import { protractor } from 'protractor';
// import {getAuthToken} from '../api/samplecall';
// import {$, browser, element, by, By, ExpectedConditions } from "protractor";
// import { Http } from '@angular/http';
// describe('Verify the Careplanner Scheduler Page', () => {


//     it('Test this function',()=>{
        
//              var token ;
//             var flow = protractor.promise.controlFlow();
            
//             flow.execute(getAuthToken).then(function(response){
//               var responseObj=response;
//               // console.log(responseObj['access_token']);
//               token=responseObj['access_token'];
//               console.log("new token: "+token);
//             }).then( () =>{
                
//                 flow.execute(getAuthToken).then(function(response){

//             })

//         });
