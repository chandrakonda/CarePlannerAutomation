import { browser } from 'protractor';
describe('Verify the Patient Header has accurate Patient and Visit information', () => {

it("",()=>{
    browser.logger.info("Verify the object");
    expect(3).toEqual(3);
})
});